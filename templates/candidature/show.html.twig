{% extends 'base.html.twig' %}

{% block title %}Candidature{% endblock %}

{% block body %}
    <h1>Candidature</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Date</th>
                <td>{{ candidature.Date ? candidature.Date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ candidature.user.id }}</td>
            </tr>
            <tr>
                <th>Annonce</th>
                <td>{{ candidature.annonce.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('candidature_index') }}">back to list</a>

    <a href="{{ path('candidature_edit', {'User': candidature.User.id}) }}">edit</a>

    {{ include('candidature/_delete_form.html.twig') }}
{% endblock %}
