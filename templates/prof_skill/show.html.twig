{% extends 'base.html.twig' %}

{% block title %}ProfSkill{% endblock %}

{% block body %}
    <h1>ProfSkill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prof_skill.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ prof_skill.Name }}</td>
            </tr>
            <tr>
                <th>Percent</th>
                <td>{{ prof_skill.Percent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prof_skill_index') }}">back to list</a>

    <a href="{{ path('prof_skill_edit', {'id': prof_skill.id}) }}">edit</a>

    {{ include('prof_skill/_delete_form.html.twig') }}
{% endblock %}
